@model IEnumerable<BookStoreWeb.Models.Domain.Author>
@{
}

<div class="container pt-5 pb-5">
    <div class="row">
        <div class="col-md-6 pb-2">
            <a href="/Author/Add" class="btn btn-dark">Add more</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>Book Author List</h3>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th scope="col">Author</th>
                            <th scope="col">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(var item in Model)
                        {
                            <tr class="table-light">
                                <td>@item.AuthorName</td>
                                <td><a class="btn btn-success" href="/Author/Update?id=@item.Id">Edit</a> | 
                                <a class="btn btn-danger" onclick="return window.confirm("Are you sure?")" href="/Author/Delete?id=@item.Id">Delete</a></td>
                            </tr>
                        }
                        
                    </tbody>
                </table>
        </div>
    </div>
</div>

