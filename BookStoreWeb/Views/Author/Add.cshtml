@model BookStoreWeb.Models.Domain.Author

<div class="container pt-5 pb-5">
    <div class="row">
        <div class="col-md-6">
            <h3>Add Book Author</h3>
            <form asp-controller="Author" asp-action="Add" method="post">
                <div class="form-group pb-2">
                    <label for="AuthorName">Author Name *</label>
                    <input type="text" class="form-control" placeholder="Enter Author Name" asp-for="AuthorName">
                    <span asp-validation-for="AuthorName" class="text-danger"></span>
                </div>
                @if (TempData["msg"] != null)
                {
                    @TempData["msg"]
                }
                <div class="form-group">
                    <button type="submit" class="btn btn-success">Save</button>
                    <a href="/Author/GetAll" class="btn btn-info">List</a>
                </div>
            </form>
        </div>
    </div>
</div>


